[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: let client-side routing handle every path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
